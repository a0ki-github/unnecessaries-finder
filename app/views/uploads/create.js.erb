// Loadingエフェクトの解除
$("body").css({ 'opacity': '1' });
$("#loading").remove();

// 結果表示
$(".container").append("<%= j(render('result_wrapper', items: @items))%>");

// 結果部分までスクロール
$("html,body").animate({scrollTop:$("#result").offset().top - $("header").outerHeight()} );

